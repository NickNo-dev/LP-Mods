WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    // SceneStart()
    
    // selectNPC()
    // MainActor = getSelectedNPC()
    // actId = MainActor.getID()
    // actName = "<MainActor.name>"
    // MainActor.delete()
    
    // SameActor = getSpecific(actId)
    // sameId = SameActor.getID()
    
    // "Main Actor: <actName> actId=<actId> ++ SameActor: <SameActor.Name> sameId=<sameId>"
    
    // MainActor = getPerson(true)
    // While MainActor.isValid()
        // actId = MainActor.getID()
        // actName = "<MainActor.name>"
        // "Main Actor: <actName> actId=<actId>"
        // MainActor = getPerson(true)
    // EndWhile
    
    // SceneEnd()
    
    
    Player.setActorVar(pe_PlayerOfficeLocationSize, 0)
    Player.setActorVar(pe_PlayerOfficePrice, 0)
    
   
    // Actor = generatePersonTemporary()
    // Actor.makePermanent()
    // exchangeContact(Actor)
    // Actor:pornfame => 95
    // Actor:attractiveness => 96
    // "Added: <Actor.name>."
    